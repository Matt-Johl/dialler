<?xml version="1.0" encoding="ISO-8859-1" ?>
<!DOCTYPE scenario SYSTEM "sipp.dtd">
<!--
  The latency probe's INVITE (ADMIN-API.md §4.7 gate): as dev-hc (213) to
  dev-hd (214), a provisioned user with no phone, so the server's answer is
  the fast 480 of the wake path and no harness phone is rung. rtd 1 is the
  authenticated INVITE to its final response.
-->
<scenario name="probe_invite">
  <send>
    <![CDATA[
      INVITE sip:214@dialler SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch];rport
      Max-Forwards: 70
      From: <sip:[service]@dialler>;tag=[pid]SIPpTag02[call_number]
      To: <sip:214@dialler>
      Call-ID: [call_id]
      CSeq: 1 INVITE
      Contact: <sip:[service]@[local_ip]:[local_port];transport=[transport]>
      Content-Type: application/sdp
      Content-Length: [len]

      v=0
      o=user1 53655765 2353687637 IN IP[local_ip_type] [local_ip]
      s=-
      c=IN IP[media_ip_type] [media_ip]
      t=0 0
      m=audio [media_port] RTP/AVP 0
      a=rtpmap:0 PCMU/8000
    ]]>
  </send>
  <recv response="401" auth="true" timeout="5000"/>
  <send>
    <![CDATA[
      ACK sip:214@dialler SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch];rport
      Max-Forwards: 70
      From: <sip:[service]@dialler>;tag=[pid]SIPpTag02[call_number]
      To: <sip:214@dialler>[peer_tag_param]
      Call-ID: [call_id]
      CSeq: 1 ACK
      Content-Length: 0
    ]]>
  </send>
  <send start_rtd="1">
    <![CDATA[
      INVITE sip:214@dialler SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch];rport
      Max-Forwards: 70
      From: <sip:[service]@dialler>;tag=[pid]SIPpTag02[call_number]
      To: <sip:214@dialler>
      Call-ID: [call_id]
      CSeq: 2 INVITE
      Contact: <sip:[service]@[local_ip]:[local_port];transport=[transport]>
      [authentication username=dev-hc password=tok_dev_hc_harness_fixed]
      Content-Type: application/sdp
      Content-Length: [len]

      v=0
      o=user1 53655765 2353687637 IN IP[local_ip_type] [local_ip]
      s=-
      c=IN IP[media_ip_type] [media_ip]
      t=0 0
      m=audio [media_port] RTP/AVP 0
      a=rtpmap:0 PCMU/8000
    ]]>
  </send>
  <recv response="100" optional="true"/>
  <recv response="180" optional="true"/>
  <recv response="480" timeout="5000" rtd="1"/>
  <send>
    <![CDATA[
      ACK sip:214@dialler SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch];rport
      Max-Forwards: 70
      From: <sip:[service]@dialler>;tag=[pid]SIPpTag02[call_number]
      To: <sip:214@dialler>[peer_tag_param]
      Call-ID: [call_id]
      CSeq: 2 ACK
      Content-Length: 0
    ]]>
  </send>
</scenario>
