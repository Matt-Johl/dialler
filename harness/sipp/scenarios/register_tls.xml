<?xml version="1.0" encoding="ISO-8859-1" ?>
<!DOCTYPE scenario SYSTEM "sipp.dtd">
<!--
  REGISTER over TLS to the light server. The app leg is Digest-authenticated
  with the enrolled device's credential (SPEC §4.4 rule 2): the first
  REGISTER is challenged (401), the second carries the device's response
  and is accepted (200 with our Contact echoed). dev-ha is enrolled for 211,
  the [service] user run.sh passes.
-->
<scenario name="register_tls">
  <send>
    <![CDATA[
      REGISTER sip:dialler SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch];rport
      Max-Forwards: 70
      From: <sip:[service]@dialler>;tag=[pid]SIPpTag00[call_number]
      To: <sip:[service]@dialler>
      Call-ID: [call_id]
      CSeq: 1 REGISTER
      Contact: <sip:[service]@[local_ip]:[local_port];transport=[transport]>;expires=300
      User-Agent: sipp-harness
      Content-Length: 0
    ]]>
  </send>

  <recv response="401" auth="true" timeout="5000"/>

  <send>
    <![CDATA[
      REGISTER sip:dialler SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch];rport
      Max-Forwards: 70
      From: <sip:[service]@dialler>;tag=[pid]SIPpTag00[call_number]
      To: <sip:[service]@dialler>
      Call-ID: [call_id]
      CSeq: 2 REGISTER
      Contact: <sip:[service]@[local_ip]:[local_port];transport=[transport]>;expires=300
      [authentication username=dev-ha password=tok_dev_ha_harness_fixed]
      User-Agent: sipp-harness
      Content-Length: 0
    ]]>
  </send>

  <recv response="200" timeout="5000">
    <action>
      <ereg regexp="expires=300" search_in="hdr" header="Contact:" check_it="true" assign_to="1"/>
    </action>
  </recv>

  <!-- SIPp rejects variables that are assigned but never read; this marks $1 as used. -->
  <Reference variables="1"/>
</scenario>
