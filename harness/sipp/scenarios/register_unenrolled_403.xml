<?xml version="1.0" encoding="ISO-8859-1" ?>
<!DOCTYPE scenario SYSTEM "sipp.dtd">
<!--
  Negative: a host on the Wi-Fi with no enrolment at all tries to register
  as [service] (201) with made-up credentials. Challenged (401), then
  refused (403). This is the exposure the app-leg authentication closes.
-->
<scenario name="register_unenrolled_403">
  <send>
    <![CDATA[
      REGISTER sip:dialler SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch];rport
      Max-Forwards: 70
      From: <sip:[service]@dialler>;tag=[pid]SIPpTag06[call_number]
      To: <sip:[service]@dialler>
      Call-ID: [call_id]
      CSeq: 1 REGISTER
      Contact: <sip:[service]@[local_ip]:[local_port];transport=[transport]>;expires=300
      User-Agent: sipp-harness
      Content-Length: 0
    ]]>
  </send>

  <recv response="401" auth="true" timeout="5000"/>

  <send>
    <![CDATA[
      REGISTER sip:dialler SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch];rport
      Max-Forwards: 70
      From: <sip:[service]@dialler>;tag=[pid]SIPpTag06[call_number]
      To: <sip:[service]@dialler>
      Call-ID: [call_id]
      CSeq: 2 REGISTER
      Contact: <sip:[service]@[local_ip]:[local_port];transport=[transport]>;expires=300
      [authentication username=intruder password=guessing_is_not_enrolment]
      User-Agent: sipp-harness
      Content-Length: 0
    ]]>
  </send>

  <recv response="403" timeout="5000"/>
</scenario>
