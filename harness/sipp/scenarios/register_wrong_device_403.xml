<?xml version="1.0" encoding="ISO-8859-1" ?>
<!DOCTYPE scenario SYSTEM "sipp.dtd">
<!--
  Negative: a valid device credential for the WRONG user. dev-hb is enrolled
  for 212; here it tries to register as [service] (211). The Digest verifies,
  but the server must refuse (403): an enrolled phone cannot register as, or
  take the calls of, another user.
-->
<scenario name="register_wrong_device_403">
  <send>
    <![CDATA[
      REGISTER sip:dialler SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch];rport
      Max-Forwards: 70
      From: <sip:[service]@dialler>;tag=[pid]SIPpTag05[call_number]
      To: <sip:[service]@dialler>
      Call-ID: [call_id]
      CSeq: 1 REGISTER
      Contact: <sip:[service]@[local_ip]:[local_port];transport=[transport]>;expires=300
      User-Agent: sipp-harness
      Content-Length: 0
    ]]>
  </send>

  <recv response="401" auth="true" timeout="5000"/>

  <send>
    <![CDATA[
      REGISTER sip:dialler SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch];rport
      Max-Forwards: 70
      From: <sip:[service]@dialler>;tag=[pid]SIPpTag05[call_number]
      To: <sip:[service]@dialler>
      Call-ID: [call_id]
      CSeq: 2 REGISTER
      Contact: <sip:[service]@[local_ip]:[local_port];transport=[transport]>;expires=300
      [authentication username=dev-hb password=tok_dev_hb_harness_fixed]
      User-Agent: sipp-harness
      Content-Length: 0
    ]]>
  </send>

  <recv response="403" timeout="5000"/>
</scenario>
